[package]
name = "text_editor"
version = "0.1.0"
edition = "2024"

[dependencies]
gl = "0.14"
rfd = "0.17.2"
sdl2 = { version = "0.38", default-features = false, features = ["ttf"] }

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = "z"
panic = "abort"
strip = true
rfd = "0.17"
